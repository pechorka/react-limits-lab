// Placeholder workloads orchestrator to satisfy runController wiring.
import type { Scenario } from '../types/scenario'

export function startAll(_scenario: Scenario) {
  // Intentionally a no-op for now.
}

export function stopAll() {
  // Intentionally a no-op for now.
}

